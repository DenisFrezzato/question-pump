Repository with example posted in [this question](https://github.com/mafintosh/pump/issues/52).

## Installation

`npm i`

## Run

`npm start`

Example request: `curl --request POST 'http://localhost:3000' --form 'file=@./package-lock.json'`
